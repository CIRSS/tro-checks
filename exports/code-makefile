# Check the checker's JavaScript against its JSDoc annotations, with the same
# configuration the editor reads. Node's own types are installed globally in
# the image, where tsc does not look unless told.
test:
	cd ${REPRO_MNT} && tsc -p jsconfig.json --typeRoots /usr/local/lib/node_modules/@types --types node
