{
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "$id": "https://w3id.org/trace/tro/0.1/has-well-formed-graph.schema.json",
    "title": "Has a well-formed graph",
    "description": "Where a document carries an @graph, its value must be a node object or an array of node objects.",
    "properties": {
        "@graph": {
            "type": ["object", "array"],
            "items": {
                "type": "object",
                "errorMessage": {
                    "type": "every member of an @graph array is a node object"
                }
            },
            "errorMessage": {
                "type": "an @graph holds a node object, or an array of node objects"
            }
        }
    }
}
